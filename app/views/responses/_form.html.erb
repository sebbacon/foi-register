<%= form_for([@response.request, @response]) do |f| %>
  <% if @response.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@response.errors.count, "error") %> prohibited this response from being saved:</h2>

      <ul>
      <% @response.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<%= render :partial => "requests/show_request_part", :locals => {:r => @response.request} %>

<% for response in @response.request.responses[0..-2] %>
 <%= render :partial => "responses/show_response_part", :locals => {:r => response} %>
<% end %>
 
 <%= render :partial => "responses/reply_fields", :locals => {:response => f} %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
