<h1>Listing responses</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @responses.each do |response| %>
  <tr>
    <td><%= link_to 'Show', [response.request, response] %></td>
    <td><%= link_to 'Edit', edit_request_response_path(response.request, response) %></td>
    <td><%= link_to 'Destroy', [response.request, response], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Response', new_request_response_path(@request) %>
